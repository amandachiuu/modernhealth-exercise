const psMapInitialState = {
    "mindfulness": 1,
    "values": 2,
    "action": 3,
    "flexibility": 4
  }
  
  function programSectionMap(state = psMapInitialState, action) {
    switch (action.type) {
      default:
        return state
    }
  }

  export default programSectionMap;